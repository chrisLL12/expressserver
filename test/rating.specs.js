// //Unit Test
// import { expect } from 'chai';
// import { getReviewById } from '../src/reviews';
//
// describe ('reviews (module with reviews function)', () => {
//     describe('rating (module that returns an id)', () => {
//         it('returns correct drink data', () => {
//             let result = getReviewById(4);
//             expect(result.name).to.equal('Double Espresso');
//             expect(result.rating).to.equal(5);
//             expect(result.featuredComment).to.equal('This espresso is really light and smooth! It\'s my regular drink.');
//         });
//         it('undefined for id other than 1-5', () => {
//             let result = getReviewById(7);
//             expect(result).to.equal(undefined);
//         });
//     });
// });